{
  "name": "@loan-platform/auth-service",
  "version": "0.0.1",
  "private": true,
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "start": "ts-node-dev --respawn src/main.ts",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "lint": "eslint \"src/**/*.{ts,tsx}\""
  },
  "dependencies": {
    "@loan-platform/shared-types": "workspace:^"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.0",
    "eslint": "^8.0.0",
    "rimraf": "^5.0.0"
  }
}

